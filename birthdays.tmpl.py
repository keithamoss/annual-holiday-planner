import datetime

from config import YEAR

BIRTHDAYS = [
  {
    "name": "This should be a one or two letter version of the person's name - so it fits in the cell nicely.",
    "date": datetime.date(YEAR, 1, 1)
  }
]